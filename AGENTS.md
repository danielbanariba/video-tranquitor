# Repository Guidelines

The guidelines for this repository live in **[CLAUDE.md](./CLAUDE.md)**. Read that file.

This one used to carry its own copy and drifted badly: it still described `src/main.ts`, the entry
point of the TypeScript v1 that was replaced by the Python package under `src/video_tranquitor/`.
Two files documenting the same thing diverge — so this is a pointer now, not a second source.
